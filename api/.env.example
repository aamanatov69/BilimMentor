DATABASE_URL="postgresql://postgres:postgres@localhost:5432/nexora_lms?schema=public"
JWT_SECRET="change-me-in-production"
PORT=4000
CORS_ALLOWED_ORIGINS="http://localhost:3000"

# Password reset email settings
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USER="no-reply@example.com"
SMTP_PASS="replace-with-smtp-password"
SMTP_FROM="BilimMentor <no-reply@example.com>"
MAILER_ALLOW_FALLBACK=false

# For Gmail SMTP: use 2FA + App Password (16 chars), not your regular account password.

# Frontend URL used to build reset-password links
# Use either FRONTEND_URL or NEXT_PUBLIC_APP_URL
FRONTEND_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"




